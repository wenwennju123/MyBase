# 简历提问与回答

## 项目1  医疗器械+美妆+社区团购平台

#### 问题：项目的整体结构，功能，模块是什么样子的

PC管理后台，微信小程序（按买家级别进行权限划分 社区医院诊所店主、医美美容院店主、社区店主、普通用户、会员用户、高级会员用户（社区代表以及二级分销））
	模块：用户门户，商家门户，商品搜索，商品展示，商品推送，购物车，订单流程，配送流程，会员中心，售后服务，帮助中心。
	模块：商品管理，订单管理，商家管理，会员管理，促销管理，广告运营管理，消息（站内信）管理，内容管理，统计报表，财务管理，权限管理，后台设置。
	商品管理细分：商品信息，商品分类，商品类型，商品品牌，销售评级，参数管理
	订单管理细分：订单信息，订单设置，退货申请，售后跟进
	营销模块：秒杀活动，优惠卷管理，品牌推荐，新品推荐，人气推荐，专题推荐，首页广告
	把专题推荐模块临时改造为疫情期间的讨论板块，可以由社区代表发起讨论，用户点击相应来统计购买意愿等
并给社区采购提供专门的按照楼栋配送分类（映射） 订单表40多个字段，选了几个用来映射，例如订单类型，订单状态

#### 问题：用户评分系统画像算法逻辑

结合用户基础信息，性别年龄，用户注册信息中也会选择填写职业，爱好等。

用户的在线时间段，浏览的内容（推荐，广告 --- 兴趣）分类，浏览的商品分类，评价分类（评价还没办法根据文字判断是好评差评，根据星级统计）购买记录。  其实用户画像是建立在商品品类足够丰富且竞品较多的情况下。是一种着眼后期发展的一个方略，可惜没发展起来。

#### 问题：什么是用户的恶意行为，如何锁定并防止。如何对高访问量--过滤--限流--分类降级

用户重复点击重复发送请求---**前端防抖**---**唯一请求ID** + **缓存响应结果**（绑定请求ID例如 "reponse:" + requestID 默认-1）结果.过期时间保持一致。10s或30s，依据业务。

对不同请求Id和响应结果的过期时间，基础上添加一定随机性，防止缓存雪崩。

**如果业务处理时间较长：**导致唯一请求ID即将过期或者已经过期的情况。解决方案：解决幂等性的是**唯一请求ID**，还可以建议一个**逻辑ID**是与**业务参数**相关的，缓存响应结果，**不直接与请求ID绑定**。另一种解决办法就是对**动态ID进行动态续期**，直到请求成功响应结果加入缓存。

评论注水---验证码机制

用户多次重复下单购买单件小额商品，标记用户并每日统一生成订单

用户重复下单并反复退货，次数超限开始拉黑降低权限

疫情期间突发的用户高额访问量，例如不断刷新商品页面，配送页面，社区论坛以获取物资最新消息。前端防抖限制1s。限流阈值1000，一直触发限流超过10s触发熔断，唯一请求ID给15s过期时间。搜商品的就降级到本地有缓存的商品，社区讨论的就加载论题初始板块？优先考虑本地缓存。

gateway的熔断器  CircuitBreaker   Resilience4j  （翻译过来时恢复能力的意思）

唯一请求ID过期时间和**熔断器时间窗口**的关系，避免新的请求使用重复的ID，过期时间回避时间窗口长。保证熔断器统计请求的成功率错误率是正确的。一般是1.5倍的关系。

 限流的请求阈值qps（超过会排队或拒绝）与熔断器请求窗口的关系。大概就是 时间窗口乘以限流阈值，就是请求窗口，要监控的请求量。然后再失败率（超时多少认定为失败）例如50%，对应请求失败数量500个时，触发熔断。

**自定义路由断言工厂**中匹配不同的路径，不同的限流策略。引用该路由断言工厂并添加Filter ：RequestRateLimiter 指定限流阈值。

**短时间削峰使用redis**进行存储，自定义过滤器，在限流触发后将请求的详细信息进行缓存，例如List。后台使用线程池或定时任务定期从redis队列中取出请求进行处理。

**长时间削峰使用RabbitMQ**进行存储，将请求信息发送到消息队列中，由后端服务从队列中异步处理。

被缓存的消息需要设置合理的**过期时间**，如果消息超时了，可以**直接丢弃并返回特定响应**给用户，提示服务器繁忙请稍后重试。

在等待响应过程中，如果用户退出了页面或者程序，如何判断**用户取消**了，如何通知服务器消费方该请求取消了无需处理。（redis标记，消息队列中移除消息）同时向用户返回取消成功的确认，避免不必要的期望。可以WebSocket长连接进行推送（不必要）。使用延迟两次确认机制。不用长连接，就需要**客户端向服务器发送取消请求的HTTP请求**。这样无论是redis状态标记，还是消息状态（取出时查一次，执行完之后再查一次，取消了就直接丢弃basicReject，没取消就basicAck）

触发了限流，部分请求移动到削峰缓存了，还可以**主动降级**，返回静态响应、默认响应并引导用户稍后重试。在RequestRateLimiter 中配置 fallbackUri

**自定义降级过滤器**：根据请求的路径来转发到不同的页面，注册该控制器，然后将 fallbackUri 指向该控制器实现自定义

#### 问题：刚刚你提到了长连接，那么什么情况需要使用长连接

长连接主要事要进行**实时数据推送**，例如客服聊天，实时抢购看到库存容量，实时竞价竞拍等。在确定必须要使用**双向连接**时才使用。这也是为什么大多数开始使用内置客服机器人，需要复杂操作才可以接人工。

#### 问题：站内信-优惠活动推送-模块是怎么搭建的，如何存储

用户界面中包含有站内信系统，如何实现。**用户**表--**关联**表--**用户消息**表。（有些关联关系的是可以分担部分业务逻辑字段，例如已读未读等，不需要查询全部的情况下，只查关系表也可以）sql操作，生成消息，发送消息，查看消息，查看全部消息。更新消息状态，删除消息，记录阅读时间等。以及消息-定时任务-定期清理归档。

#### 问题：讨论板块如何搭建（评论系统）

最初调研时的设计  淘宝 月销20w的洗护商品，评价总数在1-2w，带图的评论不超过1000，有字数的评论约占40%

表结构：**主题**表（主题id，发起主题用户id），**帖子**表（帖子id，关联主题id，发帖用户id），**评论**表（评论id，关联帖子id，发评用户id）

热点讨论版 如何限流和降级-----恶意**刷屏**--**-redis计数器**，例如每个用户**每分钟最多发帖10次**

流量高蜂--热点评论--根据用户**等级**--只有少数**高级用户可以评论**，其他是**只读**的。 ----  降级策略--分页加载--用户评论后的刷新只加载前面少数评论（或者干脆直接读取本地缓存的前几条评论）空闲时候更新评论量，点赞量排序等等。

防止注水---**审核**（利用自然语言处理技术对评论内容进行自动审核，过滤掉垃圾信息和重复评论。可以使用一些关键词过滤或机器学习模型来检测。）---次数过多需要输入**验证码**

**定时更新**主题的浏览量，定时检测热点话题热点评论

替代**websocket的实时**通知用户评论---**定时通知** + 用户主动查看时会发送**更新请求**，降低网络通信压力

#### 问题：平衡前后端缓存？本地，cdn redis都存什么，等级？过期时间？如何考虑缓存被清理的情况。

**本地缓存**：用户偏好，设置，短期数据，短期有效且复用率高的例如首页广告，商品推荐列表，用户的购物车，搜索结果等。还有一些静态资源例如图标，图片，字体等。还有JWT维持用户登陆状态用的。

**cdn缓存**：存储不频繁变化的静态内容，例如商品图，用户头像，css样式，js文件等。热门页面数据静态化放在cdn中降低压力。或者一些api接口返回的是相对静态的数据例如分类列表，配置，设置等，可以放入cdn降低压力。定时更新

**redis**：高频访问的数据（详情，库存，用户信息等），热点数据（热点搜索词，热门主题等---定时更新），临时会话，分布式锁，计数器限流器。

根据数据特点，**访问频率**，**变动性**，**安全性**来平衡缓存位置。合理的**过期时间**，**数据冷热分离**。数据一致性（双删 防止并发读导致缓存了旧数据）与**缓存更新策略**。

例如**缓存旁路模式** Cache Asaide Pattern。写操作时更新数据库同时清除缓存，读取时从优先读取缓存，缓存失效等未命中重新从数据库加载并更新缓存。

缓存穿透---查不存在的---布隆过滤器

缓存击穿--某几个高热点数据---互斥锁，队列缓冲

缓存雪崩--大量缓存短时间同时失效--失效时间加上随机性

#### 问题：与其他平台合作，支付怎么进行，如何制定api，传参与返回值。

在原有api基础上，套了一层api的壳子，一方面可以调用我们原本的api，还可以记录统计日志，收集双方需要的数据形成报表。（访问量，成交量，退货率等）

**API网关层**，（远程）调用原有api并进行数据的收集和日志记录，可以使用**拦截器**实现，也可以写入controller

分布式日志收集系统  Elasticsearch（矢量数据库 存储和索引日志数据）  Logstash（日志框架，从不同的源收集并存储到es）  Kibana（可视化分析）

配置Logback的Appender将日志发送到Logstash。通过AOP自定义注解+切面；拦截器  preHandle；或者直接调用方法logger.info(message)，来记录日志。

调用失败怎么办---重试---spring-Retry框架--注解编写（触发重试的异常，间隔时间，间隔时间倍率）

#### 问题：商品的评分逻辑

商品的总销量，月销量，好评率，好评字数大于12的比率，差评率。结合商品的出货稳定率与可降价幅度，参与平台促销优惠活动。

#### 问题：MySQL集群怎么搭建和部署的，分库，分表，分区，哪些表做了分片，自动分片的逻辑是什么，怎么保证高可用。同问技术点：MySQL NDB Cluster。

**分库**：将多个表的数据分散存储到多个数据库实例中，减轻单个数据库的负载。常见于模块化系统中。带来的挑战就是不同的库不同的数据源就会有**分布式事务**以及**跨库查询**的问题。

**分表**：将单个表中的数据分散到同一个数据库实例中的**多个表**中，减少单表数据量，提高查询效率。常见策略由**水平分表--ID、时间**和**垂直分表--常用字段--不常用字段**。带来的挑战有：自动化分表策略以及维护问题。垂直分表的连接查询问题。

**分区**：将表的数据划分为多个部分，存储在不同文件中，但是逻辑上是一个表。维护方便，支持：范围分区，列表分区，哈希分区，键分区。

**分片**：在多个数据库实例时间分布数据，在分库分表的基础上进行，根据特定的键值进行数据分布，应用在集群上，通常带有分片副本。

#### 问题：Elasticsearch在你的项目中，在哪些-地方有使用

记录日志，用户分析，行为分析，

**日志分级归档**：重要日志例如订单，支付相关的，长期保留不进行压缩归档。

次要日志用于统计的，进行**压缩打包**，例如**gzip**，压缩率在50%-60%，然后存储在磁盘或者其他介质例如oss有提供日志存储服务。

过期清理例如只保留一年内的日志，六个月以上的进行摘要处理，冗余信息移除。

分布式日志存储和分片和集群扩展。冷热分离。

ES是基于**Lucene**的搜索引擎，**全文搜索**性能很强。 索引，文档，类型，字段

录入商品搜索信息 json -- mongoDB，进行相关性搜索。---中文分词器  查询语法

编写es查询：使用**Spring Data Elasticsearch**提供的自定义查询方法。你可以使用`@Query`注解，或者直接使用**`NativeSearchQueryBuilder`**构建自定义查询。然后使用**ElasticsearchRestTemplate**来执行查询

用户查，根据es -- mongodb查到的时商品概览，也就是一堆商品列表，只显示主要的 缩略图，名称，价格，销量，活动等等。包含商品uid，再点击才会进入商品详情查uid。之后就是redis优化mysql查详情的内容等等

搜索流程：用户点击搜索栏---加载用户搜索历史记录、热点提示词、销量排行榜等等--用户输入搜索内容（热点词库联想）--后台es查找--返回List并展示商品概要信息--用户点击商品概要图等发送查找商品uid详情--后台查找并返回。

#### 问题：RabbitMQ 延迟队列 死信队列在你项目中，用在了哪里，解决了什么问题。

延迟队列：通过**TTL + 特殊死信队列**实现，也可通过插件 r**abbitmq-delayed-message-exchange**

不想立刻让消费者拿到消息消费的情况。例如**订单超时未支付自动取消**（延迟时间设置为支付限时例如一小时，支付成功就移除消息，没有支付就）**业务上的逻辑延时，和定时任务有重叠，都是实现单次延时的效果**,例如延迟订单评价提醒，促销活动开始前的预热消息，库存锁定的**超时取消**，定期数据清理，订单状态变更后的延迟通知。

死信队列：DLX **防止重复消费**，避免某些消息一直无法成功消费导致资源浪费--重试逻辑，定期进行额外的处理逻辑或者通知相关维护人员

#### 问题：讲讲表结构：mdb 

用户表 20+，基础信息，收货地址，积分系统，

订单表 40+ ：类型，状态信息，积分系统，优惠券系统，发票，地址，收货信息，

商品表 40+ ：基础信息，积分系统，成长系统，推荐系统，优惠活动，库存，

#### 问题：背景与环境：

回答：2021-2022年封禁较为严格
老板原本做仓储分销 + 医疗器械代工的，疫情里赚了点钱，意识到需要转型，转型美妆产品代工分销。购买了仓库，厂房。引进加工产线，联系线下医美，与接着之前的诊所关系。

#### 问题：物流细节：工厂车间生产细节

回答：冷链---有保存条件需求的药品与针剂
	合作的有本地的司机，天天和顺丰。
	固定基础生产 + 下定制订单给工厂 预估产能，负责人对接，产线，记录

#### 问题：后来呢

回答：疫情结束，零售、社区团购业务砍掉，重点放在美妆代工仓储分销，加入淘宝淘工厂计划。

#### 问题：展望

回答：淘宝服饰联合阿里国际的全球包邮计划----物流基建，仓储，配送部署。国际化
	对商家及商品进行更精细化的管理，扬长避短



## 项目2  办公效能软件

#### 问题：你说你对原本的sql进行了升级，举例看看，提取了哪些公共逻辑

包含不少子查询 group by语句 order by语句的情况，



#### 问题：讲讲你优化sql都优化了哪里





#### 问题：数据清洗，对账，详细讲讲你都处理了哪些问题。







#### 问题：excel批量导入导出数据库怎么实现的。

首先制定规范，限制excel表格的行和列的形式。

批量导入



#### 问题：数据图表展示，坐标系可变化的，怎么实现





#### 问题：自动识别发票，报销单。如何判断种类，是否需要人工确认和校对。







## 代码规范

#### 问题：你说使用了阿里巴巴Java开发手册进行规范，改正了哪些编码习惯，具体哪些习惯杜绝了哪些问题，

必须使用String来构建BigDecimal，避免因使用double损失精度，**因为double本身就是近似值**，想比较就是用BigDecimal的compareTo方法（忽略精度  1.0和1.00 用equals会不相等）

日期和时间格式化时，必须使用 yyyy-MM-dd HH:mm:ss  不可以使用Y 是因为 y代表year**，Y代表WeekYear 针对不同国家**的年的定义，一周的开始时不同的 国际化的标准 ISO 8601

避免因类型对齐时，自动拆箱导致的NPE，例如三目运算符

制定HashMap初始容量大小，**扩容会**重新排布重新哈希运算，大大消耗资源。并且多线程下扩容是导致**线程不安全**的主要原因。初始容量为2的倍数。建议容量是 预计插入数据除以0.75 + 1 之后最接近的最小的2 的次方整数

**禁止任何魔法值**直接出现在代码中，必须定义常量，枚举，变量

为什么不建议使用Java自带的Executors静态工厂构建线程池：使用**ThreadPoolExecutor**创建，并且指定全部的参数。指定阻塞队列的容量以避免oom。

为什么不建议在for循环中使用 + 进行字符串拼接：String是final的，每 + 一个就生成一个新的。Java不支持运算符重载，建议使用String的concat方法或者StringBuffer StringBuilder.append来拼接。  **+   运算符在反编译之后，底层使用的是StringBuilder**，是线程不安全的。 **for循环中每一次都会new一个StringBuilder**，会很占内存也降低了效率。

为什么**禁止foreach循环中进行元素的remove和add操作**：增强for底层使用的是do while 和 iterator去遍历集合，会抛异常 ConcurrentModificationException，触发了fail-fast错误检测机制。单线程违反了规则也会抛异常。抛出的方法是 checkForComodification。而元素的add和remove是集合类自己的方法，iterator是不知道的，触发 modCount  != expectedModCount。可以使用普通for来处理集合，或者使用iterator的remove方法

为什么禁止直接使用日志系统例如 Log4j、Logback中的API。而应该**使用SLF4j中的API**，使用门面模式的日志框架。有利于维护各个类的日志处理方式统一。**日志门面 Facade：Simple Logging Facade for Java**

为什么谨慎使用继承来实现扩展，推荐使用组合的方式。如果必须使用继承，那么必须符合里氏代换原则。父类能出现的地方子类一定能出现。Java代码的**复用**有 **继承**，**组合**，**代理** 三种具体的表现形式

count(*) 会包含值是null的行数，如果是count(列名) 就会剔除掉null行



## 管理协作联调

复习下mall的mdb表设计图

RationalRose绘制请求流程图





## 中间件RabbitMQ





## 中间件Redis



## 框架 SpringBoot



## 框架Spring



## 框架MyBatis



## 数据库MySQL



## 了解一下Neo4j





## JavaSE



## JVM



## AI参会你有什么见解



## 展示一下你自己的AI软件使用



## 展示一下你学习的开源项目













































