<?xml version="1.0" encoding="UTF-8"?>

<!--添加命名空间：context，aop，tx，mvc-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
                           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
                           http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--dispatcherServlet拦截所有URL请求-->
    <mvc:default-servlet-handler/>
    <!--添加组件扫描，指定包路径controller-->
    <context:component-scan base-package="com.leowork.crm.web.controller"/>
    <context:component-scan base-package="com.leowork.crm.settings.web.controller"/>
    <context:component-scan base-package="com.leowork.crm.workbench.web.controller"/>
    <!--启用mvc注解驱动-->
    <mvc:annotation-driven/>
    <!--配置视图解析器-->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--所有页面均在此目录
        首页index.jsp
        系统相关，用户登录，设置管理等  settings文件夹下
        功能相关，各种模块           workbench文件夹下
        -->
        <property name="prefix" value="/WEB-INF/pages/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--配置拦截器 拦截器链执行顺序依照配置顺序-->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--配置拦截的请求路径 访问业务资源的请求 故此之前配置url的路径，均与资源的路径相同 拦截settings与workbench的路径 即可保护业务资源-->
            <!--第一个 / 表示应用的根目录 /**包含其下的所有子目录-->
            <mvc:mapping path="/settings/**"/>
            <mvc:mapping path="/workbench/**"/>
            <!--配置放行请求 登录与登陆验证 放行的优先级更高-->
            <mvc:exclude-mapping path="/settings/qx/user/toLogin.action"/>
            <mvc:exclude-mapping path="/settings/qx/user/login.action"/>
            <!--指定拦截器类-->
            <bean class="com.leowork.crm.settings.web.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!--配置文件上传解析器-->
    <!--<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="#{1024*1024*80}"/>
        <property name="defaultEncoding" value="utf-8"/>
    </bean>-->

</beans>